package com.dinodevs.greatfitwatchface.slpt

enum class AlignX(val slptValue: Byte) {
    LEFT(0),
    RIGHT(1),
    CENTER(2),
}

enum class AlignY(val slptValue: Byte) {
    TOP(0),
    BOTTOM(1),
    CENTER(2),
}